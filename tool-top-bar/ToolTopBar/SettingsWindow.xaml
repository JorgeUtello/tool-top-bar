<Window x:Class="ToolTopBar.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuración" Height="580" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        Background="#FF1A1A1A">
    <Window.Resources>
        <!-- Estilo global para ComboBoxItem -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#FF2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#44FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Estilo global para ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#FF2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#33FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                        Grid.Column="2"
                                        Focusable="False"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition/>
                                                <ColumnDefinition Width="20"/>
                                            </Grid.ColumnDefinitions>
                                            <Border x:Name="Border" Grid.ColumnSpan="2" CornerRadius="2" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"/>
                                            <Path x:Name="Arrow" Grid.Column="1" Fill="White" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#FF3A3A3A"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="8,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#FF2A2A2A" BorderThickness="1" BorderBrush="#33FFFFFF"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TabControl Grid.Row="0" Background="#FF1A1A1A" BorderBrush="#33FFFFFF" Padding="4">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border" Background="#22FFFFFF" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="4,4,0,0" Padding="12,8" Margin="2,0">
                                    <ContentPresenter x:Name="ContentSite" ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#FF2A2A2A"/>
                                        <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#33FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Foreground" Value="#CCFFFFFF"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </TabControl.Resources>
            
            <!-- Pestaña de Opciones -->
            <TabItem Header="Opciones">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="12"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="12"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="12"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="12"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="12"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="12"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

        <!-- Alto de barra -->
        <TextBlock Text="Alto de barra (px):" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#CCFFFFFF"/>
        <Grid Grid.Column="2">
            <TextBox x:Name="BarHeightBox" 
                     Height="32"
                     Padding="8,0,32,0"
                     VerticalContentAlignment="Center"
                     Background="#FF2A2A2A"
                     Foreground="White"
                     BorderBrush="#33FFFFFF"
                     BorderThickness="1"
                     PreviewKeyDown="NumericBox_PreviewKeyDown" 
                     LostFocus="NumericBox_LostFocus"/>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0">
                <Button Content="▲" Width="20" Height="14" Padding="0" FontSize="8" Tag="BarHeightBox" Click="IncrementButton_Click" BorderThickness="0" Background="#22FFFFFF" Foreground="White"/>
                <Button Content="▼" Width="20" Height="14" Padding="0" FontSize="8" Tag="BarHeightBox" Click="DecrementButton_Click" BorderThickness="0" Background="#22FFFFFF" Foreground="White"/>
            </StackPanel>
        </Grid>
        <!-- Alto ícono -->
        <TextBlock Grid.Row="2" Text="Alto ícono (px):" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#CCFFFFFF"/>
        <Grid Grid.Row="2" Grid.Column="2">
            <TextBox x:Name="IconSizeBox" 
                     Height="32"
                     Padding="8,0,32,0"
                     VerticalContentAlignment="Center"
                     Background="#FF2A2A2A"
                     Foreground="White"
                     BorderBrush="#33FFFFFF"
                     BorderThickness="1"
                     PreviewKeyDown="NumericBox_PreviewKeyDown" 
                     LostFocus="NumericBox_LostFocus"/>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0">
                <Button Content="▲" Width="20" Height="14" Padding="0" FontSize="8" Tag="IconSizeBox" Click="IncrementButton_Click" BorderThickness="0" Background="#22FFFFFF" Foreground="White"/>
                <Button Content="▼" Width="20" Height="14" Padding="0" FontSize="8" Tag="IconSizeBox" Click="DecrementButton_Click" BorderThickness="0" Background="#22FFFFFF" Foreground="White"/>
            </StackPanel>
        </Grid>

        <!-- Espacio entre íconos -->
        <TextBlock Grid.Row="4" Text="Espacio entre íconos (px):" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#CCFFFFFF"/>
        <Grid Grid.Row="4" Grid.Column="2">
            <TextBox x:Name="IconSpacingBox" 
                     Height="32"
                     Padding="8,0,32,0"
                     VerticalContentAlignment="Center"
                     Background="#FF2A2A2A"
                     Foreground="White"
                     BorderBrush="#33FFFFFF"
                     BorderThickness="1"
                     PreviewKeyDown="NumericBox_PreviewKeyDown" 
                     LostFocus="NumericBox_LostFocus"/>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0">
                <Button Content="▲" Width="20" Height="14" Padding="0" FontSize="8" Tag="IconSpacingBox" Click="IncrementButton_Click" BorderThickness="0" Background="#22FFFFFF" Foreground="White"/>
                <Button Content="▼" Width="20" Height="14" Padding="0" FontSize="8" Tag="IconSpacingBox" Click="DecrementButton_Click" BorderThickness="0" Background="#22FFFFFF" Foreground="White"/>
            </StackPanel>
        </Grid>

        <!-- Margen horizontal -->
        <TextBlock Grid.Row="6" Text="Margen horizontal (px):" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#CCFFFFFF"/>
        <Grid Grid.Row="6" Grid.Column="2">
            <TextBox x:Name="EdgePaddingHBox" 
                     Height="32"
                     Padding="8,0,32,0"
                     VerticalContentAlignment="Center"
                     Background="#FF2A2A2A"
                     Foreground="White"
                     BorderBrush="#33FFFFFF"
                     BorderThickness="1"
                     PreviewKeyDown="NumericBox_PreviewKeyDown" 
                     LostFocus="NumericBox_LostFocus"/>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0">
                <Button Content="▲" Width="20" Height="14" Padding="0" FontSize="8" Tag="EdgePaddingHBox" Click="IncrementButton_Click" BorderThickness="0" Background="#22FFFFFF" Foreground="White"/>
                <Button Content="▼" Width="20" Height="14" Padding="0" FontSize="8" Tag="EdgePaddingHBox" Click="DecrementButton_Click" BorderThickness="0" Background="#22FFFFFF" Foreground="White"/>
            </StackPanel>
        </Grid>

        <!-- Margen vertical -->
        <TextBlock Grid.Row="8" Text="Margen vertical (px):" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#CCFFFFFF"/>
        <Grid Grid.Row="8" Grid.Column="2">
            <TextBox x:Name="EdgePaddingVBox" 
                     Height="32"
                     Padding="8,0,32,0"
                     VerticalContentAlignment="Center"
                     Background="#FF2A2A2A"
                     Foreground="White"
                     BorderBrush="#33FFFFFF"
                     BorderThickness="1"
                     PreviewKeyDown="NumericBox_PreviewKeyDown" 
                     LostFocus="NumericBox_LostFocus"/>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0">
                <Button Content="▲" Width="20" Height="14" Padding="0" FontSize="8" Tag="EdgePaddingVBox" Click="IncrementButton_Click" BorderThickness="0" Background="#22FFFFFF" Foreground="White"/>
                <Button Content="▼" Width="20" Height="14" Padding="0" FontSize="8" Tag="EdgePaddingVBox" Click="DecrementButton_Click" BorderThickness="0" Background="#22FFFFFF" Foreground="White"/>
            </StackPanel>
        </Grid>

        <!-- Posición vertical íconos -->
        <TextBlock Grid.Row="10" Text="Posición vertical íconos (px):" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#CCFFFFFF"/>
        <Grid Grid.Row="10" Grid.Column="2">
            <TextBox x:Name="IconTopOffsetBox" 
                     Height="32"
                     Padding="8,0,32,0"
                     VerticalContentAlignment="Center"
                     Background="#FF2A2A2A"
                     Foreground="White"
                     BorderBrush="#33FFFFFF"
                     BorderThickness="1"
                     PreviewKeyDown="NumericBox_PreviewKeyDown" 
                     LostFocus="NumericBox_LostFocus"/>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0">
                <Button Content="▲" Width="20" Height="14" Padding="0" FontSize="8" Tag="IconTopOffsetBox" Click="IncrementButton_Click" BorderThickness="0" Background="#22FFFFFF" Foreground="White"/>
                <Button Content="▼" Width="20" Height="14" Padding="0" FontSize="8" Tag="IconTopOffsetBox" Click="DecrementButton_Click" BorderThickness="0" Background="#22FFFFFF" Foreground="White"/>
            </StackPanel>
        </Grid>

        <!-- Posición -->
        <TextBlock Grid.Row="12" Text="Posición:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#CCFFFFFF"/>
        <ComboBox x:Name="BarEdgeCombo" 
                  Grid.Row="12" 
                  Grid.Column="2" 
                  Height="32"
                  VerticalContentAlignment="Center"
                  Background="#FF2A2A2A"
                  Foreground="White"
                  BorderBrush="#33FFFFFF">
            <ComboBoxItem Tag="Left">Izquierda</ComboBoxItem>
            <ComboBoxItem Tag="Top">Arriba</ComboBoxItem>
            <ComboBoxItem Tag="Right">Derecha</ComboBoxItem>
        </ComboBox>

        <!-- CheckBox -->
        <Border Grid.Row="14" Grid.ColumnSpan="3" 
                Background="#FF2A2A2A" 
                BorderBrush="#33FFFFFF" 
                BorderThickness="1" 
                CornerRadius="4" 
                Padding="12,8">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Mostrar la barra en:" Margin="0,0,0,4" FontWeight="SemiBold" Foreground="#CCFFFFFF"/>
                <ComboBox x:Name="DisplayModeCombo" Height="28" SelectionChanged="DisplayModeCombo_SelectionChanged" Background="#FF2A2A2A" Foreground="White" BorderBrush="#33FFFFFF">
                    <ComboBoxItem Tag="PrimaryOnly">Solo pantalla principal</ComboBoxItem>
                    <ComboBoxItem Tag="OnePerScreen">Una barra por cada pantalla</ComboBoxItem>
                </ComboBox>
                <CheckBox x:Name="HideVDBtnsCheckBox" 
                          Content="Ocultar botones de escritorio virtual" 
                          Foreground="#CCFFFFFF"
                          FontWeight="Normal" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>
                    </Grid>
                </ScrollViewer>
            </TabItem>
            
            <!-- Pestaña Programas Activos -->
            <TabItem Header="Programas Activos">
                <Grid Background="#FF1A1A1A">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Stretch">
                        <TextBlock Text="Filtrar programas que se muestran en la ventana activa:" Foreground="#CCFFFFFF" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        <Button x:Name="RefreshProcessesButton" Content="Refrescar" Width="90" Height="28" Margin="12,0,0,0" Click="RefreshProcesses_Click" Background="#22FFFFFF" Foreground="White" BorderBrush="#33FFFFFF"/>
                        <Button x:Name="SelectAllProcessesButton" Content="Seleccionar todo" Width="120" Height="28" Margin="8,0,0,0" Click="SelectAllProcesses_Click" Background="#22FFFFFF" Foreground="White" BorderBrush="#33FFFFFF"/>
                    </StackPanel>

                    <Border Grid.Row="2" Background="#FF2A2A2A" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="4" Padding="8">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="ProcessItemsControl">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="4">
                                            <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" Foreground="#CCFFFFFF" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding DisplayName}" Foreground="#CCFFFFFF" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>

                    <TextBlock Grid.Row="4" Text="Nota: la lista muestra procesos con ventana principal (aplicaciones GUI). Marca los que quieras ver." Foreground="#99FFFFFF" FontSize="12"/>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Botones -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,12,0,0">
            <Button Content="Salir" 
                    Width="90" 
                    Height="36"
                    Click="Exit_Click"
                    Background="#D13438"
                    Foreground="White"
                    BorderThickness="0"/>
            <Button Content="Cancelar" 
                    Width="90" 
                    Height="36"
                    Margin="0,0,8,0" 
                    Click="Cancel_Click"
                    Background="#FF2A2A2A"
                    Foreground="White"
                    BorderBrush="#33FFFFFF"
                    BorderThickness="1"/>
            <Button Content="Guardar" 
                    Width="90" 
                    Height="36"
                    IsDefault="True" 
                    Margin="0,0,8,0" 
                    Click="Save_Click"
                    Background="#0078D4"
                    Foreground="White"
                    BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
